git init
create repo in github app. else use github cli and commands to create repo using command.
create readme
git add README.md
git commit -m "first commit"
git branch -M main    (-M for renaming main branch(default given by git))
git remote add origin https://github.com/Aayushi595/LifeLink.git
git push -u origin main  (Sets origin/main as the upstream for your local main)

